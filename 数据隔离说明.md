# 用户数据隔离问题修复说明

## 🚨 发现的问题

### **问题描述：**
用户 A 登录下单后，切换到用户 B 登录，用户 B 的"我的订单"中显示了用户 A 的订单。

### **问题原因：**

1. **订单数据未按用户隔离**
   - 订单存储在 localStorage 中使用固定的 key: `'orders'`
   - 所有用户共享同一个订单列表
   - 切换用户后，localStorage 中的订单数据没有清除

2. **未从服务器获取订单**
   - Orders.vue 页面没有在 onMounted 中调用 `fetchOrders()`
   - 直接显示本地缓存的订单（可能是其他用户的）

3. **登出时未清理数据**
   - 用户登出时没有清空订单、地址等个人数据
   - 下一个用户登录时会看到上一个用户的数据

### **安全风险：**
- ⚠️ **用户隐私泄露**：其他用户可以看到不属于自己的订单
- ⚠️ **操作权限混乱**：尝试取消别人的订单时报错

---

## ✅ 解决方案

### **1. 订单数据按用户隔离**

**修改文件**: `src/stores/order.js`

**修改内容**:
```javascript
// 获取当前用户的订单存储 key
function getStorageKey() {
  const userStore = JSON.parse(localStorage.getItem('user') || '{}')
  const userId = userStore.user?.id || userStore.user?.username || 'guest'
  return `heytea_orders_${userId}`  // ✅ 每个用户独立的 key
}

// 加载订单（从 localStorage，按用户隔离）
function loadOrders() {
  const key = getStorageKey()  // ✅ 使用用户专属 key
  const stored = localStorage.getItem(key)
  if (stored) {
    orders.value = JSON.parse(stored)
  } else {
    orders.value = []
  }
}

// 保存订单（到 localStorage，按用户隔离）
function saveOrders() {
  const key = getStorageKey()  // ✅ 使用用户专属 key
  localStorage.setItem(key, JSON.stringify(orders.value))
}
```

**效果**:
- 用户 A 的订单存储在 `heytea_orders_user_a`
- 用户 B 的订单存储在 `heytea_orders_user_b`
- 互不干扰 ✅

---

### **2. 添加清空数据方法**

**修改文件**: 
- `src/stores/order.js` - 添加 `clearOrders()`
- `src/stores/address.js` - 添加 `clearAddresses()`

**代码**:
```javascript
// 清空订单（用户登出时调用）
function clearOrders() {
  orders.value = []
  const key = getStorageKey()
  localStorage.removeItem(key)
  console.log(`🗑️ 已清空订单缓存 (key: ${key})`)
}
```

---

### **3. 用户登出时清理所有数据**

**修改文件**: `src/stores/user.js`

**修改内容**:
```javascript
async function logout() {
  try {
    // 调用退出 API
    if (token.value) {
      await authApi.logout()
    }
  } catch (err) {
    console.error('退出登录 API 调用失败:', err)
  } finally {
    // 🛒 禁用购物车同步
    const cartStore = useCartStore()
    cartStore.disableSync()
    
    // 📦 清空订单数据 ✅ 新增
    const orderStore = useOrderStore()
    orderStore.clearOrders()
    
    // 🏠 清空地址数据 ✅ 新增
    const addressStore = useAddressStore()
    addressStore.clearAddresses()
    
    // 清除登录信息
    user.value = null
    token.value = ''
    localStorage.removeItem('token')
    localStorage.removeItem('user')
    
    console.log('✅ 用户已登出，所有数据已清空')
  }
}
```

---

### **4. 订单列表页从服务器加载**

**修改文件**: `src/views/Orders.vue`

**修改内容**:
```javascript
onMounted(async () => {
  console.log('📋 订单列表页加载，开始获取订单...')
  try {
    // ✅ 从服务器获取订单，而不是读取本地缓存
    await orderStore.fetchOrders()
    console.log(`✅ 订单列表加载完成: ${orders.value.length} 个订单`)
  } catch (err) {
    console.error('❌ 加载订单列表失败:', err)
    toast.error('加载订单列表失败，请稍后重试')
  }
})
```

---

### **5. 移除初始化时的自动加载**

**修改文件**: `src/stores/order.js`

**修改前**:
```javascript
// 初始化时加载订单
loadOrders()  // ❌ 问题：会加载上一个用户的订单
```

**修改后**:
```javascript
// ⚠️ 不要在初始化时自动加载本地缓存
// 订单数据应该在用户登录后从服务器获取
// loadOrders()  // ✅ 已移除
```

---

## 📊 修复后的流程

### **用户 A 的操作：**

```
1. 用户 A 登录
   └─ Token: token_a
   └─ User ID: user_a
   
2. 用户 A 下单
   └─ 订单存储在: heytea_orders_user_a
   └─ 服务器也保存了订单
   
3. 用户 A 登出
   └─ 清空 orders: []
   └─ 删除 localStorage: heytea_orders_user_a
   └─ 清空购物车、地址等所有数据 ✅
```

### **用户 B 的操作：**

```
1. 用户 B 登录
   └─ Token: token_b
   └─ User ID: user_b
   
2. 进入"我的订单"页面
   └─ onMounted 调用 fetchOrders()
   └─ 从服务器获取用户 B 的订单 ✅
   └─ 只显示属于用户 B 的订单 ✅
   
3. 用户 B 看不到用户 A 的订单 ✅
```

---

## 🔒 安全性提升

### **修复前：**
❌ 所有用户共享订单数据
❌ 切换用户后数据未清理
❌ 可以看到其他用户的订单（隐私泄露）

### **修复后：**
✅ 每个用户独立的订单存储
✅ 登出时清空所有个人数据
✅ 订单列表从服务器获取（基于用户 Token）
✅ 后端验证订单权限（返回 404 而不是其他用户的订单）

---

## 🧪 测试步骤

### **测试 1：验证数据隔离**

1. 登录用户 A: `user / 123456`
2. 下单，查看"我的订单"
3. 记录订单数量: X 个
4. 打开浏览器控制台，查看 localStorage:
   ```javascript
   localStorage.getItem('heytea_orders_user')  // 或对应的 user ID
   ```
5. 登出
6. 查看 localStorage，确认订单数据已清空 ✅
7. 登录用户 B: `lisi / 123456`
8. 查看"我的订单"
9. **预期结果**: 
   - ✅ 只显示用户 B 的订单
   - ✅ 不显示用户 A 的订单

---

### **测试 2：验证服务器获取**

1. 登录用户 A
2. 下单
3. 打开控制台，查看日志:
   ```
   📋 订单列表页加载，开始获取订单...
   ✅ 从服务器加载订单: X 个订单
   ```
4. 刷新页面
5. **预期结果**:
   - ✅ 每次刷新都从服务器获取订单
   - ✅ 不依赖本地缓存

---

### **测试 3：验证登出清理**

1. 登录用户 A
2. 下单，添加地址
3. 打开控制台，查看 localStorage:
   ```javascript
   Object.keys(localStorage).filter(k => k.startsWith('heytea_'))
   // 应该看到: ['heytea_orders_user', 'heytea_cart', ...]
   ```
4. 点击"退出登录"
5. 查看控制台日志:
   ```
   ✅ 订单数据已清空
   ✅ 地址数据已清空
   ✅ 用户已登出，所有数据已清空
   ```
6. 再次查看 localStorage
7. **预期结果**:
   - ✅ 所有 `heytea_orders_*` 数据已清空
   - ✅ 用户相关的所有数据已清除

---

## 📝 其他数据存储的建议

### **购物车（已实现）**
- ✅ 使用用户专属 key: `heytea_cart_${userId}`
- ✅ 登出时清空

### **地址（已修复）**
- ✅ 完全从服务器获取
- ✅ 登出时清空

### **用户信息**
- ✅ 存储在 `localStorage.user`
- ✅ 登出时清空

---

## 🎯 总结

### **关键修改：**

1. ✅ 订单按用户隔离存储
2. ✅ 登出时清空所有数据
3. ✅ 订单列表从服务器获取
4. ✅ 移除初始化时的自动加载

### **安全性提升：**

- 🔒 用户隐私得到保护
- 🔒 数据不会泄露给其他用户
- 🔒 后端权限验证正常工作

### **用户体验提升：**

- 📱 登录后立即看到自己的订单
- 📱 切换用户流畅无错误
- 📱 数据一致性更好（以服务器为准）

---

## 🚀 后续优化建议

1. **购物车也按用户隔离** ✅（已实现）
2. **所有个人数据都使用用户专属 key**
3. **考虑使用 IndexedDB 替代 localStorage**（容量更大）
4. **添加数据版本控制**（方便迁移和清理）
5. **定期清理过期的本地数据**

---

**最后更新**: 2026-07-23
**问题状态**: ✅ 已修复
**安全级别**: 🔒 高
