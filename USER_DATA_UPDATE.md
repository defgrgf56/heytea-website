# 🔄 用户数据更新说明

## 📢 重要提醒

系统已移除管理员角色，所有用户均为普通用户。

### ✅ 已更新的内容

1. **默认用户数据**
   - `admin` 用户的昵称从"管理员"改为"用户一"
   - 所有用户均为普通用户，无特殊权限

2. **代码更新**
   - `src/api/auth.js` - 更新默认用户数据
   - `MOCK_DATA_GUIDE.md` - 更新文档说明
   - `API_INTEGRATION.md` - 更新API示例

---

## 🔧 如何应用更新

### 方法1: 重置用户数据（推荐）

如果您之前已经登录过，本地可能还保存着旧的用户数据。需要重置：

1. 打开浏览器控制台（按 F12）
2. 执行以下命令：
   ```javascript
   window.resetMockUsers()
   ```
3. 刷新页面（F5）
4. 重新登录

### 方法2: 清除浏览器缓存

1. 打开浏览器开发者工具（F12）
2. 进入 **Application** 标签页
3. 找到 **Local Storage**
4. 选择你的网站（localhost:3001）
5. 删除 `heytea_mock_users` 项
6. 刷新页面

### 方法3: 清除所有本地数据

```javascript
// 在浏览器控制台执行
localStorage.clear()
location.reload()
```

---

## 📋 更新后的默认账号

| 用户名 | 密码 | 邮箱 | 昵称 | 说明 |
|--------|------|------|------|------|
| admin | 123456 | admin@heytea.com | 用户一 | 普通用户 |
| zhangsan | 123456 | zhangsan@heytea.com | 张三 | 普通用户 |
| lisi | 123456 | lisi@heytea.com | 李四 | 普通用户 |

---

## 🎯 验证更新

登录后检查：

1. 使用 `admin` 账号登录
2. 查看右上角用户信息，应该显示"用户一"
3. 如果还显示"管理员"，说明需要重置本地数据

---

## ❓ 常见问题

### Q: 为什么我登录后还是显示"管理员"？
A: 这是因为本地保存了旧的用户数据。请按照上面的方法重置用户数据。

### Q: 重置后我的新注册用户会丢失吗？
A: 是的，`window.resetMockUsers()` 会删除所有新注册的用户，恢复为默认的3个账号。如果想保留新用户，请使用手动编辑的方式。

### Q: 有没有不丢失新用户的更新方法？
A: 可以手动编辑：
1. F12 → Application → Local Storage
2. 找到 `heytea_mock_users`
3. 点击编辑，将 `"nickname":"管理员"` 改为 `"nickname":"用户一"`
4. 保存并刷新页面

---

**更新日期**: 2026-07-23  
**影响范围**: 用户数据展示，无功能变更
