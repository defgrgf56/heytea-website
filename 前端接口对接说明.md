# 茶饮练习前端接口对接说明

> 本文档是给 `heytea-website` 和 `heytea-admin` 前端开发者的独立接口协议。你只需要拿到自己的前端项目和本文件即可开发，不需要查看 `public-practice-api` 后端源码。线上接口已经部署，所有请求、字段、权限和错误处理以本文档为准。

## 给前端开发者的直接信息

- 线上接口：`https://haonan.online/api/tea-demo/v1`
- 普通顾客账号：`user / 123456`
- 后台超级管理员：`admin / 123456`
- 商品运营：`productop / 123456`
- 订单客服：`service / 123456`
- 门店管理员：`storemgr / 123456`
- 成功响应统一为 `{ success: true, message, data }`；失败响应统一为 `{ success: false, message, code, details }`。
- 浏览器前端必须使用线上 HTTPS 地址，不要把本机绝对路径写入项目。

## 两个前端要实现的闭环

`heytea-website`：普通用户浏览商品、登录、购物车、地址、提交订单、查看订单状态。

`heytea-admin`：后台工作台、商品/分类、订单、顾客、门店，以及按角色显示菜单和按钮。

真实闭环是：官网下单 -> 后台看到同一订单 -> 后台推进状态 -> 官网刷新看到最新状态。前端不需要实现真实支付，`mock_wechat` 和 `mock_alipay` 只是教学标识。

| 项目 | 内容 |
|---|---|
| 文档版本 | `v2.0` |
| 接口版本 | `v1` |
| 更新日期 | `2026-08-07` |
| Base URL | `https://haonan.online/api/tea-demo/v1` |
| 通信协议 | `HTTPS` |
| JSON 编码 | `UTF-8` |
| 认证方式 | `Bearer Token` |

本文档是前后端联调的正式协议。除文件上传和 CSV 导出外，请求与响应均使用 `application/json`。

## 1. 通用约定

### 1.1 请求头

公开接口不需要 Token。登录后的接口统一携带：

```http
Authorization: Bearer <token>
Content-Type: application/json
```

### 1.2 成功响应

```json
{
  "success": true,
  "message": "success",
  "data": {}
}
```

业务数据只从 `data` 读取。创建接口通常返回 `201`，查询和更新通常返回 `200`。

### 1.3 失败响应

```json
{
  "success": false,
  "message": "没有该接口的权限",
  "code": "FORBIDDEN",
  "details": null
}
```

前端必须同时判断 HTTP 状态码与 `success`。`message` 用于用户提示，`code` 用于程序分支，不要根据中文提示判断业务类型。

### 1.4 分页响应

列表接口统一接受 `page`、`pageSize`，返回：

```json
{
  "success": true,
  "message": "success",
  "data": {
    "list": [],
    "total": 18,
    "page": 1,
    "pageSize": 20,
    "totalPages": 1
  }
}
```

| 参数 | 类型 | 默认值 | 限制 | 说明 |
|---|---|---|---|---|
| `page` | number | `1` | 大于等于 1 | 页码 |
| `pageSize` | number | `20` | 1 到 100 | 每页数量 |
| `keyword` | string | - | 最多 100 字符 | 模糊搜索 |
| `startDate` | string | - | `YYYY-MM-DD` | 开始日期 |
| `endDate` | string | - | `YYYY-MM-DD` | 结束日期 |

### 1.5 ID 与时间

- 资源 ID 是 24 位 MongoDB ObjectId，例如 `66b1e8c304da2123db1e0011`。
- 时间字段是 ISO 8601 字符串，例如 `2026-08-07T03:30:00.000Z`。
- 金额单位为人民币元，类型为 number，例如 `29.5`。
- 前端不得用列表数组下标代替资源 ID。

## 2. 登录加密协议

### 2.1 安全说明

登录、注册、修改密码均禁止把密码作为普通 JSON 明文直接提交。协议为：

1. 前端获取一次性 Challenge。
2. 后端返回 `challengeId`、`nonce`、RSA 公钥和过期时间。
3. 前端用 `RSA-OAEP`、`SHA-256` 加密完整凭据。
4. 前端只提交 Base64 密文。
5. Challenge 默认 2 分钟过期，成功或失败使用一次后即失效，不能重放。
6. 公钥可以公开；私钥只存在于服务端进程内存，不提供给前端。
7. 数据库只保存 bcrypt 密码哈希，不保存明文或可逆密文。

这里不使用“前后端硬编码同一个对称密钥”。把固定密钥写进浏览器代码等同于公开密钥，不能提供真正的密码保护。

### 2.2 获取 Challenge

```http
GET /auth/challenge?purpose=login
```

| 参数 | 必填 | 可选值 |
|---|---|---|
| `purpose` | 是 | `login`、`register`、`change-password` |

响应 `data`：

```json
{
  "challengeId": "8a3db1a5-4c9d-4c48-a8d0-1a2a7e1b6d77",
  "nonce": "D9TzRXYaej4C7kG8P1nC5A",
  "purpose": "login",
  "expiresAt": "2026-08-07T04:00:00.000Z",
  "algorithm": "RSA-OAEP-SHA256",
  "publicKey": "-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----\n"
}
```

### 2.3 浏览器加密封装

以下代码可直接放入前端 API 层。登录和注册使用 `POST`，修改密码使用 `PUT`。

```js
const API_BASE_URL = 'https://haonan.online/api/tea-demo/v1'

function pemToArrayBuffer(pem) {
  const base64 = pem
    .replace('-----BEGIN PUBLIC KEY-----', '')
    .replace('-----END PUBLIC KEY-----', '')
    .replace(/\s+/g, '')
  const binary = atob(base64)
  return Uint8Array.from(binary, char => char.charCodeAt(0)).buffer
}

function arrayBufferToBase64(buffer) {
  const bytes = new Uint8Array(buffer)
  let binary = ''
  for (const byte of bytes) binary += String.fromCharCode(byte)
  return btoa(binary)
}

async function request(path, options = {}) {
  const token = localStorage.getItem('teaDemoToken')
  const response = await fetch(`${API_BASE_URL}${path}`, {
    ...options,
    headers: {
      ...(options.body instanceof FormData ? {} : { 'Content-Type': 'application/json' }),
      ...(token ? { Authorization: `Bearer ${token}` } : {}),
      ...options.headers
    }
  })
  const contentType = response.headers.get('content-type') || ''
  const result = contentType.includes('application/json') ? await response.json() : await response.blob()
  if (!response.ok || (contentType.includes('application/json') && !result.success)) {
    const error = new Error(result.message || `HTTP ${response.status}`)
    error.status = response.status
    error.code = result.code
    error.details = result.details
    throw error
  }
  return contentType.includes('application/json') ? result.data : result
}

async function encryptCredential(purpose, fields) {
  const challenge = await request(`/auth/challenge?purpose=${purpose}`)
  const key = await crypto.subtle.importKey(
    'spki',
    pemToArrayBuffer(challenge.publicKey),
    { name: 'RSA-OAEP', hash: 'SHA-256' },
    false,
    ['encrypt']
  )
  const plainText = JSON.stringify({
    purpose,
    challengeId: challenge.challengeId,
    nonce: challenge.nonce,
    ...fields
  })
  const encrypted = await crypto.subtle.encrypt(
    { name: 'RSA-OAEP' },
    key,
    new TextEncoder().encode(plainText)
  )
  return {
    challengeId: challenge.challengeId,
    payload: arrayBufferToBase64(encrypted)
  }
}

export async function login(identifier, password) {
  const credential = await encryptCredential('login', { identifier, password })
  return request('/auth/login', {
    method: 'POST',
    body: JSON.stringify({ credential })
  })
}

export async function register(fields) {
  const credential = await encryptCredential('register', fields)
  return request('/auth/register', {
    method: 'POST',
    body: JSON.stringify({ credential })
  })
}

export async function changePassword(oldPassword, newPassword) {
  const credential = await encryptCredential('change-password', { oldPassword, newPassword })
  return request('/auth/password', {
    method: 'PUT',
    body: JSON.stringify({ credential })
  })
}

export { request }
```

## 3. 联调账号与权限

### 3.1 后台账号

| 用户名 | 初始密码 | 角色 | 主要范围 |
|---|---|---|---|
| `admin` | `123456` | `SUPER_ADMIN` | 全部接口、系统管理 |
| `productop` | `123456` | `PRODUCT_OPERATOR` | 工作台、商品、分类、上传 |
| `service` | `123456` | `ORDER_SERVICE` | 工作台、订单、顾客、地址、导出 |
| `storemgr` | `123456` | `STORE_MANAGER` | 工作台、门店、上传 |

普通顾客联调账号：`user / 123456`。普通顾客不能访问任何 `/admin/**` 接口。

账号仅供本系统教学联调，不对应其他网站或系统账号。第一次登录后可以调用加密改密接口。

### 3.2 权限码

| 模块 | 权限码 |
|---|---|
| 工作台 | `dashboard:view` |
| 商品 | `product:view`、`product:create`、`product:update`、`product:delete`、`product:publish` |
| 分类 | `category:view`、`category:manage` |
| 订单 | `order:view`、`order:update`、`order:export` |
| 顾客和地址 | `customer:view`、`customer:update`、`customer:export` |
| 门店 | `store:view`、`store:create`、`store:update`、`store:delete` |
| 管理员 | `staff:view`、`staff:manage` |
| 角色 | `role:view`、`role:manage` |
| 菜单 | `menu:view`、`menu:manage` |
| 操作日志 | `operation-log:view` |
| 文件上传 | `upload:image` |

前端权限用于菜单、路由和按钮展示；后端仍会独立校验权限。隐藏按钮不能代替接口鉴权。

## 4. 认证接口

| 方法 | 路径 | 登录 | 说明 |
|---|---|---|---|
| `GET` | `/auth/challenge` | 否 | 获取一次性加密 Challenge |
| `POST` | `/auth/register` | 否 | 注册普通顾客账号 |
| `POST` | `/auth/login` | 否 | 登录后台账号或普通顾客 |
| `GET` | `/auth/me` | 是 | 获取当前账号、角色和权限 |
| `GET` | `/auth/menus` | 后台账号 | 获取当前账号的动态菜单树 |
| `PUT` | `/auth/profile` | 是 | 修改昵称、邮箱或头像 |
| `PUT` | `/auth/password` | 是 | RSA 加密修改密码 |
| `POST` | `/auth/refresh` | 是 | 刷新 Token |
| `POST` | `/auth/logout` | 是 | 退出并使当前旧 Token 失效 |

### 4.1 注册加密前字段

| 字段 | 类型 | 必填 | 规则 |
|---|---|---|---|
| `username` | string | 是 | 2 到 32 字符，全系统唯一 |
| `email` | string | 是 | 有效邮箱，全系统唯一 |
| `password` | string | 是 | 6 到 72 字符 |
| `nickname` | string | 否 | 最多 40 字符 |
| `avatar` | string | 否 | 最多 500 字符 |

### 4.2 登录加密前字段

```json
{
  "identifier": "admin",
  "password": "123456"
}
```

`identifier` 可以是用户名或邮箱。最终请求体固定为：

```json
{
  "credential": {
    "challengeId": "Challenge 编号",
    "payload": "Base64 编码的 RSA 密文"
  }
}
```

登录响应 `data`：

```json
{
  "token": "eyJ...",
  "tokenType": "Bearer",
  "expiresIn": "2h",
  "user": {
    "id": "66b1e8c304da2123db1e0011",
    "username": "productop",
    "nickname": "商品运营",
    "email": "product@tea-demo.local",
    "avatar": "/images/default-avatar.png",
    "accountType": "staff",
    "role": "staff",
    "roles": ["PRODUCT_OPERATOR"],
    "permissions": ["dashboard:view", "product:view", "product:create"],
    "status": "active"
  }
}
```

### 4.3 当前菜单

`GET /auth/menus` 根据启用角色、角色 `menuCodes` 和权限码返回树形菜单。按钮权限不放在菜单树中，按钮根据 `/auth/me` 的 `permissions` 判断。

```json
[
  {
    "code": "business",
    "name": "业务管理",
    "type": "DIRECTORY",
    "path": "/business",
    "children": [
      {
        "code": "products",
        "name": "商品管理",
        "type": "MENU",
        "path": "/products",
        "componentKey": "ProductList",
        "permission": "product:view"
      }
    ]
  }
]
```

### 4.4 资料与改密

修改资料：

```http
PUT /auth/profile
```

```json
{
  "nickname": "新的昵称",
  "email": "new@example.com",
  "avatar": "https://example.com/avatar.png"
}
```

修改密码时，加密前字段为：

```json
{
  "oldPassword": "123456",
  "newPassword": "NewPassword#2026"
}
```

修改成功后所有旧 Token 失效，前端应清除本地登录状态并重新登录。

## 5. 公开业务接口

| 方法 | 路径 | 查询参数 | 说明 |
|---|---|---|---|
| `GET` | `/health` | - | 服务健康检查 |
| `GET` | `/product-categories` | - | 启用商品分类，首项为 `all` |
| `GET` | `/products` | `page`、`pageSize`、`keyword`、`categoryCode`、`isHot`、`isNew` | 在售和售罄商品列表 |
| `GET` | `/products/:id` | - | 商品详情与规格 |
| `GET` | `/stores` | `page`、`pageSize`、`keyword`、`city` | 公开门店列表 |
| `GET` | `/stores/:id` | - | 公开门店详情 |

### 5.1 商品结构

```json
{
  "id": "66b1e8c304da2123db1e0011",
  "name": "多肉葡萄",
  "nameEn": "Grape Tea",
  "description": "鲜葡萄果肉与茶底",
  "descriptionEn": "Fresh grape tea",
  "price": 29,
  "categoryCode": "fruit",
  "categoryName": "水果茶",
  "imageUrl": "https://images.unsplash.com/...",
  "bannerImages": ["https://images.unsplash.com/..."],
  "isNew": false,
  "isHot": true,
  "status": "ON_SALE",
  "stock": 999,
  "sortOrder": 10,
  "specs": {
    "sizes": [{ "code": "m", "name": "中杯", "extraPrice": 0 }],
    "sweetness": [{ "code": "less", "name": "少糖" }],
    "toppings": [{ "code": "pearls", "name": "珍珠", "price": 3 }]
  }
}
```

为兼容已有展示页面，响应同时保留 `image/imageUrl`、`desc/description`、`category/categoryCode`、`sort/sortOrder`。新代码优先使用后一个正式字段。

商品状态：`ON_SALE` 在售、`OFF_SALE` 下架、`SOLD_OUT` 售罄。

## 6. 普通顾客业务接口

本章接口全部要求普通顾客 Token。后台工作人员 Token 访问返回 `403 FORBIDDEN`；资源不属于当前顾客时统一按不存在返回 `404`，避免泄露其他顾客数据。

### 6.1 购物车

| 方法 | 路径 | 请求参数 | 说明 |
|---|---|---|---|
| `GET` | `/cart` | - | 获取当前顾客购物车和实时价格 |
| `POST` | `/cart/items` | 商品、数量、规格编码 | 添加或合并相同商品规格 |
| `PUT` | `/cart/items/:id` | `{ "quantity": 2 }` | 修改购物车项数量，范围 1 到 20 |
| `DELETE` | `/cart/items/:id` | - | 删除一项 |
| `DELETE` | `/cart` | - | 清空购物车，幂等 |

添加购物车项：

```json
{
  "productId": "66b1e8c304da2123db1e0011",
  "quantity": 2,
  "sizeCode": "m",
  "sweetnessCode": "less",
  "toppingCodes": ["pearls"]
}
```

为兼容官网已有 API 封装，也接受 `size`、`sweetness`、`toppings`，新代码统一使用带 `Code/Codes` 的正式字段。相同商品、杯型、甜度和配料组合会合并数量。每个顾客最多保留 50 种规格组合，单项最多 20 件。

购物车响应：

```json
{
  "items": [
    {
      "id": "购物车项 ID",
      "productId": "商品 ID",
      "name": "多肉葡萄",
      "imageUrl": "https://...",
      "sizeCode": "m",
      "sweetnessCode": "less",
      "toppingCodes": ["pearls"],
      "specs": "中杯 / 少糖 / 珍珠",
      "quantity": 2,
      "unitPrice": 32,
      "price": 32,
      "subtotal": 64,
      "available": true,
      "unavailableReason": ""
    }
  ],
  "totalItems": 2,
  "availableItemCount": 2,
  "goodsAmount": 64,
  "totalPrice": 64,
  "hasUnavailableItems": false
}
```

价格和可售状态每次由服务端按当前商品重新计算。失效购物车项仍会返回，但 `available=false` 并提供 `unavailableReason`；创建订单时只要存在失效项就会拒绝整单，前端应提示顾客处理购物车。

### 6.2 顾客收货地址

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/addresses` | 当前顾客地址列表，默认地址排在首位 |
| `GET` | `/addresses/default` | 默认地址；没有地址时 `data` 为 `null` |
| `GET` | `/addresses/:id` | 本人地址详情 |
| `POST` | `/addresses` | 新增地址；首个地址自动成为默认地址 |
| `PUT` | `/addresses/:id` | 部分字段更新 |
| `DELETE` | `/addresses/:id` | 软删除；删除默认地址后自动选择剩余地址 |
| `PUT` | `/addresses/:id/default` | 设置唯一默认地址 |

正式地址字段：

```json
{
  "receiverName": "张三",
  "receiverPhone": "13800138000",
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "detailAddress": "科技园联调路 8 号",
  "label": "公司",
  "isDefault": true
}
```

为兼容官网当前表单，也接受 `name`、`phone`、`detail`；响应同时返回正式字段和这些兼容字段。前端新代码优先使用正式字段。

### 6.3 顾客订单

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/orders` | 本人订单分页；支持 `page`、`pageSize`、`status` |
| `POST` | `/orders` | 从服务端购物车创建真实订单 |
| `GET` | `/orders/:id` | 本人订单详情和状态历史 |
| `PUT` | `/orders/:id/cancel` | 顾客取消 `pending` 或 `confirmed` 订单 |
| `POST` | `/orders/:id/reorder` | 将仍可购买的历史商品重新加入购物车 |

创建订单请求：

```json
{
  "addressId": "66b1e8c304da2123db1e0099",
  "payMethod": "mock_wechat",
  "remark": "请放前台",
  "clientRequestId": "checkout-20260808-a1b2c3d4"
}
```

- `payMethod` 仅支持教学占位值 `mock_wechat`、`mock_alipay`，不会调用真实支付。
- `clientRequestId` 必填，8 到 80 位，只允许字母、数字、下划线和连字符。同一顾客重复提交相同编号时返回原订单，不重复扣库存。
- 创建订单不接收或不信任前端的商品名、商品价格、配送费和总金额。后端读取购物车和商品规格，重新计算成交单价，固定配送费为 `5` 元。
- 成功创建订单后扣减商品库存、保存商品和规格快照并清空购物车。商品后续改名或调价不会改变历史订单。
- 用户或后台首次取消真实订单时恢复库存；重复取消或演示种子订单不会重复增加库存。
- `reorder` 返回更新后的购物车和 `skipped` 数组。历史商品已下架、删除、规格失效或库存不足时只跳过对应商品，不直接生成新订单。

订单状态与后台订单管理共用同一模型和流转规则。这里的 `pending` 统一解释为“待确认”，不是“待支付”。用户端刷新订单详情后可以看到后台最新处理状态。

## 7. 工作台接口

全部接口要求后台账号和 `dashboard:view`。

| 方法 | 路径 | 参数 | 返回内容 |
|---|---|---|---|
| `GET` | `/admin/dashboard/summary` | - | 今日订单/销售额、待处理订单、商品/顾客/门店数 |
| `GET` | `/admin/dashboard/sales-trend` | `days` 或 `startDate/endDate` | 日期、订单数、销售额趋势 |
| `GET` | `/admin/dashboard/order-distribution` | - | 订单状态分布 |
| `GET` | `/admin/dashboard/top-products` | `limit`，1 到 20 | 热销商品 |
| `GET` | `/admin/dashboard/recent-orders` | `limit`，1 到 20 | 最近订单 |

## 8. 商品与分类管理

### 8.1 商品接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/products` | `product:view` | 管理端商品分页，含下架商品 |
| `GET` | `/admin/products/:id` | `product:view` | 管理端商品详情 |
| `POST` | `/admin/products` | `product:create` | 新增商品 |
| `PUT` | `/admin/products/:id` | `product:update` | 部分字段更新 |
| `PATCH` | `/admin/products/:id/status` | `product:publish` | 修改商品状态 |
| `PATCH` | `/admin/products/batch-status` | `product:publish` | 批量修改状态，最多 100 条 |
| `DELETE` | `/admin/products/:id` | `product:delete` | 删除商品 |

新增商品请求：

```json
{
  "name": "青提限定茶",
  "nameEn": "Seasonal Grape Tea",
  "description": "青提果肉与清香茶底",
  "descriptionEn": "Fresh grape with tea",
  "price": 29,
  "categoryCode": "fruit",
  "imageUrl": "https://images.unsplash.com/...",
  "bannerImages": ["https://images.unsplash.com/..."],
  "isNew": true,
  "isHot": false,
  "status": "ON_SALE",
  "stock": 200,
  "sortOrder": 50,
  "specs": {
    "sizes": [{ "code": "m", "name": "中杯", "nameEn": "Medium", "extraPrice": 0 }],
    "sweetness": [{ "code": "less", "name": "少糖", "nameEn": "Less Sugar" }],
    "toppings": [{ "code": "pearls", "name": "珍珠", "nameEn": "Pearls", "price": 3 }]
  }
}
```

`PUT` 支持只提交需要修改的正式字段。分类必须存在且处于启用状态。

批量状态：

```json
{
  "ids": ["66b1e8c304da2123db1e0011"],
  "status": "OFF_SALE"
}
```

### 8.2 分类接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/categories` | `category:view` | 分类分页，含商品数量 |
| `GET` | `/admin/categories/all` | `category:view` | 下拉框分类；`includeInactive=true` 可含停用项 |
| `GET` | `/admin/categories/:id` | `category:view` | 分类详情 |
| `POST` | `/admin/categories` | `category:manage` | 新增分类 |
| `PUT` | `/admin/categories/:id` | `category:manage` | 修改分类 |
| `PATCH` | `/admin/categories/:id/status` | `category:manage` | 启用/停用 |
| `PATCH` | `/admin/categories/:id/sort` | `category:manage` | 修改排序 |
| `DELETE` | `/admin/categories/:id` | `category:manage` | 删除空分类 |

```json
{
  "code": "seasonal",
  "name": "季节限定",
  "nameEn": "Seasonal",
  "icon": "Calendar",
  "sortOrder": 90,
  "status": "active"
}
```

分类编码创建后不可修改。分类状态为 `active` 或 `inactive`。存在关联商品时删除返回 `409 CATEGORY_IN_USE`。

## 9. 订单管理

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/orders` | `order:view` | 订单分页 |
| `GET` | `/admin/orders/statistics` | `order:view` | 今日及状态统计 |
| `GET` | `/admin/orders/export` | `order:export` | 按筛选条件导出 CSV |
| `GET` | `/admin/orders/:id` | `order:view` | 订单详情、商品和状态历史 |
| `PATCH` | `/admin/orders/:id/status` | `order:update` | 执行合法状态流转 |

列表筛选：`keyword`、`status`、`userId`、`startDate`、`endDate`、`page`、`pageSize`。

状态修改请求：

```json
{ "status": "confirmed" }
```

### 9.1 状态流转

```text
pending -> confirmed -> preparing -> delivering -> completed
   |           |            |
   +-----------+------------+-> cancelled
```

| 状态 | 说明 | 可变更为 |
|---|---|---|
| `pending` | 待确认 | `confirmed`、`cancelled` |
| `confirmed` | 已确认 | `preparing`、`cancelled` |
| `preparing` | 制作中 | `delivering`、`cancelled` |
| `delivering` | 配送中 | `completed` |
| `completed` | 已完成 | 不可再变更 |
| `cancelled` | 已取消 | 不可再变更 |

非法跳转返回 `409 INVALID_ORDER_STATUS_TRANSITION`，前端收到后应刷新订单详情。

## 10. 顾客与地址

### 10.1 顾客接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/customers` | `customer:view` | 顾客分页及消费汇总 |
| `GET` | `/admin/customers/export` | `customer:export` | 导出 CSV |
| `GET` | `/admin/customers/:id` | `customer:view` | 顾客资料、统计和地址 |
| `GET` | `/admin/customers/:id/orders` | `customer:view` | 顾客订单分页 |
| `GET` | `/admin/customers/:id/statistics` | `customer:view` | 订单数、累计/平均消费 |
| `GET` | `/admin/customers/:id/addresses` | `customer:view` | 顾客地址 |
| `PATCH` | `/admin/customers/:id/status` | `customer:update` | 启用或禁用账号 |
| `PATCH` | `/admin/customers/:id/tags` | `customer:update` | 修改标签，最多 10 个 |

当前前端骨架使用的旧路径继续兼容，语义与 `/admin/customers` 相同。新页面建议使用 `customers` 命名。

| 兼容方法 | 兼容路径 |
|---|---|
| `GET` | `/admin/users` |
| `GET` | `/admin/users/export` |
| `GET` | `/admin/users/:id` |
| `GET` | `/admin/users/:id/orders` |
| `GET` | `/admin/users/:id/statistics` |
| `GET` | `/admin/users/:id/addresses` |
| `PATCH` | `/admin/users/:id/status` |
| `PATCH` | `/admin/users/:id/tags` |

```json
{ "status": "disabled" }
```

```json
{ "tags": ["vip", "active"] }
```

禁用顾客后其旧 Token 立即失效。教学后台不提供顾客硬删除和明文重置密码，避免破坏订单关联或传输明文密码。

### 10.2 地址接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/addresses` | `customer:view` | 地址分页 |
| `GET` | `/admin/addresses/statistics` | `customer:view` | 地址、用户和默认地址数量 |
| `GET` | `/admin/addresses/provinces` | `customer:view` | 省份选项 |
| `GET` | `/admin/addresses/cities?province=` | `customer:view` | 城市选项 |
| `GET` | `/admin/addresses/districts?city=` | `customer:view` | 区县选项 |
| `GET` | `/admin/addresses/export` | `customer:export` | 导出 CSV |
| `GET` | `/admin/addresses/:id` | `customer:view` | 地址详情 |
| `DELETE` | `/admin/addresses/:id` | `customer:update` | 软删除地址 |

地址列表支持 `keyword`、`userId`、`province`、`city` 和分页参数。

## 11. 门店与上传

### 11.1 门店接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/stores` | `store:view` | 门店分页 |
| `GET` | `/admin/stores/all` | `store:view` | 全部门店下拉项 |
| `GET` | `/admin/stores/cities` | `store:view` | 门店城市 |
| `GET` | `/admin/stores/:id` | `store:view` | 门店详情 |
| `POST` | `/admin/stores` | `store:create` | 新增门店 |
| `PUT` | `/admin/stores/:id` | `store:update` | 修改门店 |
| `PATCH` | `/admin/stores/:id/status` | `store:update` | 修改营业状态 |
| `DELETE` | `/admin/stores/:id` | `store:delete` | 软删除门店 |

```json
{
  "name": "深圳科技园店",
  "image": "https://images.unsplash.com/...",
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "address": "科技园南区 10 栋 A 座",
  "latitude": 22.5431,
  "longitude": 114.0579,
  "phone": "0755-12345678",
  "openTime": "09:00",
  "closeTime": "22:00",
  "status": "open",
  "features": "支持自提和外卖",
  "parking": "园区地下停车场"
}
```

门店状态：`open` 营业、`closed` 关闭、`maintenance` 暂停营业。

### 11.2 图片上传

```http
POST /admin/uploads/images
Content-Type: multipart/form-data
Authorization: Bearer <token>
```

| 字段 | 类型 | 必填 | 规则 |
|---|---|---|---|
| `file` | File | 是 | JPG、PNG、WEBP，最大 2 MB |

兼容路径：`POST /admin/stores/upload`，协议完全相同。

响应 `data`：

```json
{
  "url": "https://haonan.online/api/tea-demo/v1/uploads/1723000000-uuid.png",
  "originalName": "store.png",
  "size": 102400,
  "mimeType": "image/png"
}
```

上传 `FormData` 时不要手工设置 `Content-Type`，浏览器会自动附加 multipart boundary。

## 12. 管理员与角色

### 12.1 管理员接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/staff` | `staff:view` | 后台管理员分页 |
| `GET` | `/admin/staff/:id` | `staff:view` | 管理员详情 |
| `POST` | `/admin/staff` | `staff:manage` | 新增管理员并生成临时密码 |
| `PUT` | `/admin/staff/:id` | `staff:manage` | 修改资料 |
| `PATCH` | `/admin/staff/:id/status` | `staff:manage` | 启用/禁用 |
| `PUT` | `/admin/staff/:id/roles` | `staff:manage` | 分配角色 |
| `POST` | `/admin/staff/:id/reset-password` | `staff:manage` | 生成新的临时密码 |

新增请求不接收前端自定义明文密码：

```json
{
  "username": "reporter",
  "email": "reporter@example.com",
  "nickname": "报表账号",
  "roleCodes": ["REPORT_VIEWER"],
  "status": "active"
}
```

新增响应 `data`：

```json
{
  "staff": { "id": "...", "username": "reporter", "roles": ["REPORT_VIEWER"] },
  "temporaryPassword": "Tea#随机密码9a"
}
```

临时密码只在创建或重置响应中出现一次。前端应弹窗展示给管理员，不写入日志。不能禁用自己，不能禁用或移除最后一个有效超级管理员。

### 12.2 角色接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/roles` | `role:view` | 角色分页及管理员数量 |
| `GET` | `/admin/roles/all` | `role:view` | 启用角色下拉项 |
| `GET` | `/admin/roles/:id` | `role:view` | 角色详情 |
| `POST` | `/admin/roles` | `role:manage` | 新增角色 |
| `PUT` | `/admin/roles/:id` | `role:manage` | 修改名称、说明、状态、排序 |
| `PATCH` | `/admin/roles/:id/status` | `role:manage` | 启用/停用 |
| `PUT` | `/admin/roles/:id/permissions` | `role:manage` | 分配权限与菜单 |
| `DELETE` | `/admin/roles/:id` | `role:manage` | 删除未使用的自定义角色 |

角色编码格式为大写字母、数字、下划线，例如 `REPORT_VIEWER`，创建后不可修改。

```json
{
  "permissions": ["dashboard:view", "product:view"],
  "menuCodes": ["dashboard", "business", "products"]
}
```

未知权限或菜单编码返回 400。角色权限修改后，相关管理员旧 Token 失效，必须重新登录。内置角色不能删除，被管理员使用的角色不能删除。

## 13. 菜单与操作日志

### 13.1 菜单接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/menus/tree` | `menu:view` | 完整菜单树，默认包含停用项 |
| `GET` | `/admin/menus/:id` | `menu:view` | 菜单详情 |
| `POST` | `/admin/menus` | `menu:manage` | 新增目录、菜单或按钮 |
| `PUT` | `/admin/menus/:id` | `menu:manage` | 修改菜单 |
| `PATCH` | `/admin/menus/:id/status` | `menu:manage` | 启用/停用 |
| `PATCH` | `/admin/menus/:id/sort` | `menu:manage` | 修改排序 |
| `DELETE` | `/admin/menus/:id` | `menu:manage` | 删除无子项的自定义菜单 |

菜单类型：`DIRECTORY` 目录、`MENU` 页面、`BUTTON` 按钮权限。

```json
{
  "code": "teaching-report",
  "parentCode": "business",
  "name": "教学报表",
  "type": "MENU",
  "path": "/reports/teaching",
  "componentKey": "TeachingReport",
  "icon": "DataAnalysis",
  "permission": "dashboard:view",
  "sortOrder": 90,
  "hidden": false,
  "status": "active"
}
```

`componentKey` 是前后端约定的组件映射键，不是任意文件路径。前端维护固定白名单映射，不能直接用服务端字符串动态 import 任意文件。

### 13.2 操作日志

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/admin/operation-logs` | `operation-log:view` | 操作日志分页 |
| `GET` | `/admin/operation-logs/:id` | `operation-log:view` | 日志详情 |

筛选参数：`page`、`pageSize`、`staffId`、`module`、`action`、`success=true/false`、`startDate`、`endDate`。

后台写操作和导出操作自动记录账号、角色、模块、请求方法、路径、目标 ID、成功状态、HTTP 状态、错误说明、IP、浏览器信息和时间。日志只读，不提供前端新增、编辑和删除接口。

## 14. CSV 导出

订单、顾客和地址导出接口直接返回 `text/csv; charset=utf-8`，不是统一 JSON 包装。文件包含 UTF-8 BOM，Excel 可直接识别中文。

```js
const blob = await request('/admin/orders/export?status=completed')
const url = URL.createObjectURL(blob)
const link = document.createElement('a')
link.href = url
link.download = 'orders.csv'
link.click()
URL.revokeObjectURL(url)
```

每次最多导出 5000 条。前端文件扩展名应使用 `.csv`，不要写成 `.xlsx`。

## 15. HTTP 状态码与错误码

### 15.1 HTTP 状态码

| 状态码 | 含义 | 前端处理 |
|---|---|---|
| `200` | 查询或修改成功 | 读取 `data` |
| `201` | 创建或上传成功 | 读取新资源 |
| `400` | 参数或请求格式错误 | 显示字段提示 |
| `401` | 未登录、Token/密码无效 | 清除 Token，跳转登录 |
| `403` | 账号禁用、CORS 或权限不足 | 显示无权限，不重复登录 |
| `404` | 资源或接口不存在 | 返回列表或显示空状态 |
| `409` | 业务状态冲突 | 显示 `message` 并刷新数据 |
| `422` | 上传文件类型或大小错误 | 提示重新选择文件 |
| `423` | 登录失败过多，账号临时锁定 | 等待后重试 |
| `429` | 请求过于频繁 | 按 `Retry-After` 延迟 |
| `500` | 服务端异常 | 提示稍后重试并保留请求信息 |

### 15.2 常见业务错误码

| 错误码 | 含义 |
|---|---|
| `VALIDATION_ERROR` | 参数校验失败 |
| `UNAUTHORIZED` | 未登录或 Token 已失效 |
| `FORBIDDEN` | 无接口权限 |
| `INVALID_CREDENTIALS` | 账号、密码或当前密码不正确 |
| `AUTH_CHALLENGE_EXPIRED` | Challenge 过期、无效或已使用 |
| `ACCOUNT_LOCKED` | 登录失败次数过多 |
| `USER_DISABLED` | 账号已禁用 |
| `USERNAME_EXISTS`、`EMAIL_EXISTS` | 用户名或邮箱已存在 |
| `INVALID_ID`、`VALIDATION_ERROR` | ID 或查询参数格式错误 |
| `CATEGORY_NOT_FOUND`、`CATEGORY_IN_USE` | 分类不可用或正在被商品使用 |
| `PRODUCT_NOT_FOUND` | 商品不存在 |
| `PRODUCT_NOT_ON_SALE`、`PRODUCT_SPEC_UNAVAILABLE` | 商品下架、售罄或所选规格已经失效 |
| `INSUFFICIENT_STOCK` | 商品库存不足 |
| `CART_EMPTY`、`CART_ITEM_NOT_FOUND` | 购物车为空或购物车项不存在 |
| `CART_ITEM_LIMIT_EXCEEDED`、`CART_ITEM_TYPES_LIMIT_EXCEEDED` | 购物车数量超过教学系统限制 |
| `ORDER_NOT_FOUND` | 订单不存在 |
| `INVALID_ORDER_STATUS_TRANSITION` | 订单状态不允许跳转 |
| `ORDER_CANNOT_BE_CANCELLED` | 顾客不能取消当前状态的订单 |
| `CUSTOMER_NOT_FOUND` | 顾客不存在 |
| `ADDRESS_NOT_FOUND` | 地址不存在 |
| `STORE_NOT_FOUND` | 门店不存在或不对外展示 |
| `ROLE_NOT_FOUND`、`ROLE_IN_USE` | 角色不存在或正在使用 |
| `INVALID_PERMISSION`、`INVALID_MENU_CODE` | 权限或菜单编码不存在 |
| `LAST_SUPER_ADMIN` | 操作会导致没有有效超级管理员 |
| `CANNOT_DISABLE_SELF` | 不能修改自己的启用状态 |
| `BUILT_IN_ROLE`、`BUILT_IN_MENU` | 内置数据不能删除 |
| `MENU_HAS_CHILDREN` | 菜单存在子节点 |
| `FILE_REQUIRED` | 未提交文件 |
| `UNSUPPORTED_FILE_TYPE` | 图片格式不支持 |
| `FILE_TOO_LARGE` | 图片超过 2 MB |
| `RATE_LIMITED` | 请求超过限流阈值 |
| `CORS_ORIGIN_DENIED` | 浏览器来源不在白名单 |

## 16. 前端接入顺序

1. 配置 Base URL，不要在每个 API 文件重复拼接域名。
2. 完成统一请求实例、响应解包和错误处理。
3. 接入 Challenge 与 RSA 登录，不要直接提交 `{ username, password }`。
4. 保存 Token，调用 `/auth/me` 获取账号、角色和按钮权限。
5. 调用 `/auth/menus` 生成后台菜单；普通顾客不调用该接口。
6. 完成路由守卫：401 退出登录，403 显示无权限。
7. 按商品分类、商品、订单、顾客地址、门店顺序接入业务页。
8. 最后接入管理员、角色、菜单和操作日志，验证不同岗位的菜单与按钮差异。
9. 导出按 Blob 处理，上传按 FormData 处理。
10. 用四个后台账号交叉验证，不要只用超级管理员测试。

## 17. CORS 与联调说明

服务端默认允许常用本地 Vite 地址，例如 `http://localhost:5173` 和 `http://127.0.0.1:5173`。浏览器 Origin 必须在服务端白名单中。

- Postman、Apifox 和服务端脚本通常不受浏览器 CORS 限制。
- 浏览器报 CORS 时先确认实际 Origin，包括协议、域名和端口。
- 不要在前端使用 `mode: 'no-cors'`，它会让响应不可读。
- 本地 HTTPS 页面不能请求 HTTP 接口；本项目统一使用线上 HTTPS Base URL。
- 新开发端口或部署域名需要由后端加入白名单后才能浏览器联调。

健康检查：

```http
GET https://haonan.online/api/tea-demo/v1/health
```

预期：

```json
{
  "success": true,
  "message": "success",
  "data": {
    "service": "tea-demo-api",
    "status": "ok",
    "time": "2026-08-07T03:30:00.000Z"
  }
}
```
